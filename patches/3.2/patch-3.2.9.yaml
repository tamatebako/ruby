# ruby 3.2.9 patch manifest (canonical tfs-ruby layout).
# Entries apply in listed order. Schema: schema/patches.schema.yml.
version: "3.2.9"
patches:
  # rubyw.exe needs wWinMain (LPWSTR) against the modern mingw CRT.
  # upstream paired MUNICODE_FLAG on the rubyw link line with the
  # wmain-based win32/winmain.c at 3.2.7; the patch lives in the .7+
  # overlays, never the line base (3.2.4-3.2.6 ship the old main()-based
  # file and link fine unpatched). Missing here broke the v0.2.22 windows
  # pin legs (undefined wWinMain at the rubyw.exe link).
  - feature: winmain_c_wwinmain_msys
    file: winmain_c_wwinmain_msys_7.patch
  # bundled openssl gem 3.1.0 sets V_FLAG_CRL_CHECK_ALL on the default
  # store; OpenSSL 3.6 hard-fails CRL-DP chains (tebako-runtime-ruby#123).
  # removed upstream (ruby/openssl#949); vendored ssl.rb fixed >= 3.2.10
  - feature: openssl_ssl_default_store_crl_removal
    file: ../3.1/openssl_ssl_default_store_crl_removal.patch
