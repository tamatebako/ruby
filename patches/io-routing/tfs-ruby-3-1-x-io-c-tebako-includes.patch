# tfs-ruby-3-1-x-io-c-tebako-includes.patch
#
# Target:  io.c (pass2 (linux gnu/musl, macos))
# Applies: ruby 3.1.x (3.1.6)
# Source:  tebako gem lib/tebako/packager/pass2_patch.rb Pass2Patch#io_c_patch
#
# Inject the tebako memfs io headers into io.c (non-msys).
# X-Redesign: modern-api
diff --git a/io.c b/io.c
index 1bfd39d..9e3fb97 100644
--- a/io.c
+++ b/io.c
@@ -174,6 +174,12 @@ off_t __syscall(quad_t number, ...);
 #define IO_RBUF_CAPA_FOR(fptr) (NEED_READCONV(fptr) ? IO_CBUF_CAPA_MIN : IO_RBUF_CAPA_MIN)
 #define IO_WBUF_CAPA_MIN  8192
 
+/* -- Start of tebako patch -- */
+#include <tebako/tebako-config.h>
+#include <tebako/tebako-defines.h>
+#include <tebako/tebako-io.h>
+/* -- End of tebako patch -- */
+
 /* define system APIs */
 #ifdef _WIN32
 #undef open
