# io_c_tebako_includes.patch
#
# Target:  io.c (pass2 (linux gnu/musl, macos))
# Source:  tebako gem lib/tebako/packager/pass2_patch.rb Pass2Patch#io_c_patch
#
# Route ruby's io into the tebako memfs via the modern libtfs C API
# (<tebako/fs/c_api.h>, tebako_fs_*): paths under the compiled-in
# fs_mount_point (tebako_mount_point()) go to the memfs, everything else to
# the host fs. The memfs is read-only: a write open on an in-mount path
# fails EROFS (POSIX) rather than falling through to the host open, which
# answered ENOENT for a path that does not exist there and defeated the
# callers' read-only-fs degradation (bundler's ProcessLock rescue).
# File-descriptor calls dispatch on tebako_fd_is_embedded() (the c_api FD
# introspection), so host pipes/sockets keep their native behavior.
# libtfs c_api extension (requested): tebako_fs_pread -- a positional read
# that does not move the file position (IO#pread on memfs files).
# X-Redesign: modern-api
diff --git a/io.c b/io.c
index 1bfd39d..9e3fb97 100644
--- a/io.c
+++ b/io.c
@@ -174,6 +174,193 @@
 #define IO_RBUF_CAPA_FOR(fptr) (NEED_READCONV(fptr) ? IO_CBUF_CAPA_MIN : IO_RBUF_CAPA_MIN)
 #define IO_WBUF_CAPA_MIN  8192
 
+/* -- Start of tebako patch -- */
+#include <tebako/tebako-main.h> /* tebako_mount_point(): the compiled-in fs_mount_point */
+#include <string.h>               /* strlen/strncmp/memset/memcpy used below */
+#include <errno.h>                /* ENOENT for the host-passthrough fall-through */
+#include <tebako/fs/c_api.h>     /* modern libtfs C API (tebako_fs_*) */
+
+/* Route check: does the TFS mount table claim this path? (v2 multi-mount:
+   the driver mounts the env image at the runtime root AND each payload at
+   its declared point, so the v1 mount-point prefix check is replaced by the
+   c_api coverage query. A covered path the mounts do not hold answers
+   ENOENT from the c_api -- jail-gated host passthrough -- and the wrappers
+   below pass it through to the host fs.) */
+static int
+tfs_memfs_path_p(const char *path)
+{
+    if (path == NULL) return 0;
+    return tebako_path_is_embedded(path);
+}
+
+static int
+tfs_open(const char *path, int flags, ...)
+{
+    va_list ap;
+    mode_t mode = 0;
+    int fd;
+
+    if (tfs_memfs_path_p(path)) {
+        fd = tebako_fs_open(path, flags);
+        /* Held content answers in kind: a read open serves the fd, a write
+           open fails EROFS (the mounts are read-only -- bundler's
+           ProcessLock degradation rescues exactly that). ENOENT means the
+           mounts cover but do not hold the path -- a host path, and the
+           c_api has already jail-checked it: pass through to the host
+           open. */
+        if (fd >= 0 || errno != ENOENT) return fd;
+    }
+    va_start(ap, flags);
+    if (flags & O_CREAT) {
+        mode = (mode_t) va_arg(ap, int);
+    }
+    va_end(ap);
+    return open(path, flags, mode);
+}
+
+static ssize_t
+tfs_read(int fd, void *buf, size_t nbyte)
+{
+    if (tebako_fd_is_embedded(fd)) {
+        return tebako_fs_read(fd, buf, nbyte);
+    }
+    return read(fd, buf, nbyte);
+}
+
+/* libtfs c_api extension (requested): tebako_fs_pread -- a positional read
+   that does not move the file position; declared here so this translation
+   unit stays C99-clean meanwhile */
+ssize_t tebako_fs_pread(int fd, void *buf, size_t nbyte, off_t offset);
+
+static ssize_t
+tfs_pread(int fd, void *buf, size_t nbyte, off_t offset)
+{
+    if (tebako_fd_is_embedded(fd)) {
+        return tebako_fs_pread(fd, buf, nbyte, offset);
+    }
+    return pread(fd, buf, nbyte, offset);
+}
+
+static off_t
+tfs_lseek(int fd, off_t offset, int whence)
+{
+    if (tebako_fd_is_embedded(fd)) {
+        return tebako_fs_lseek(fd, offset, whence);
+    }
+    return lseek(fd, offset, whence);
+}
+
+static int
+tfs_close(int fd)
+{
+    if (tebako_fd_is_embedded(fd)) {
+        return tebako_fs_close(fd);
+    }
+    return close(fd);
+}
+
+static int
+tfs_fstat(int fd, struct stat *st)
+{
+    if (tebako_fd_is_embedded(fd)) {
+        /* the boundary buffer is the HOST type; convert the pinned
+           tebako_stat the c_api fills into it (SSOT: c_api.h) */
+        struct tebako_stat stb;
+        int rc = tebako_fs_fstat(fd, &stb);
+        if (rc == 0) {
+            memset(st, 0, sizeof(*st));
+            st->st_mode = stb.st_mode;
+            st->st_size = stb.st_size;
+            st->st_mtime = stb.st_mtime;
+            st->st_nlink = stb.st_nlink;
+        }
+        return rc;
+    }
+    return fstat(fd, st);
+}
+
+
+/* fcntl on a memfs fd: the memfs is read-only, so the flag state is
+   emulated (FD_CLOEXEC stored, status flags report O_RDONLY); calls on host
+   fds pass through to the real fcntl */
+static int
+tfs_fcntl(int fd, int cmd, ...)
+{
+    static int cloexec_flags;
+    va_list ap;
+    int arg = 0;
+
+    va_start(ap, cmd);
+    arg = va_arg(ap, int);
+    va_end(ap);
+
+    if (!tebako_fd_is_embedded(fd)) {
+        return fcntl(fd, cmd, arg);
+    }
+#ifndef F_GETFL
+#define F_GETFL 3 /* mingw: ruby's win32.h keeps F_GETFL inside #if 0 */
+#endif
+
+    switch (cmd) {
+    case F_GETFD:
+        return cloexec_flags;
+    case F_SETFD:
+        cloexec_flags = arg;
+        return 0;
+    case F_GETFL:
+        return O_RDONLY;
+    case F_SETFL:
+        return 0;
+    default:
+        errno = EINVAL;
+        return -1;
+    }
+}
+
+#ifdef HAVE_FCOPYFILE
+/* fcopyfile(3) on a memfs src fd: the kernel cannot see embedded fds (the
+   c_api hands out flagged virtual descriptors), so the copy is emulated as
+   a read/write loop. The dst fd is always a host fd -- the memfs is
+   read-only. The COPIED byte count is reported through the state, which
+   ruby's copy_stream reads back. */
+static int
+tfs_fcopyfile(int sfd, int dfd, copyfile_state_t state, copyfile_flags_t flags)
+{
+    char buf[65536];
+    off_t copied = 0;
+    ssize_t n;
+
+    if (!tebako_fd_is_embedded(sfd)) {
+        return fcopyfile(sfd, dfd, state, flags);
+    }
+    while ((n = tebako_fs_read(sfd, buf, sizeof(buf))) > 0) {
+        ssize_t done = 0;
+
+        while (done < n) {
+            ssize_t w = write(dfd, buf + done, (size_t)(n - done));
+            if (w < 0) {
+                if (errno == EINTR) continue;
+                return -1;
+            }
+            done += w;
+        }
+        copied += n;
+    }
+    if (n < 0) return -1;
+    if (state != NULL) copyfile_state_set(state, COPYFILE_STATE_COPIED, &copied);
+    return 0;
+}
+#endif
+#define open(...) tfs_open(__VA_ARGS__)
+#define read(...) tfs_read(__VA_ARGS__)
+#define pread(...) tfs_pread(__VA_ARGS__)
+#define lseek(...) tfs_lseek(__VA_ARGS__)
+#define close(...) tfs_close(__VA_ARGS__)
+#define fstat(...) tfs_fstat(__VA_ARGS__)
+#define fcntl(...) tfs_fcntl(__VA_ARGS__)
+#define fcopyfile(...) tfs_fcopyfile(__VA_ARGS__)
+/* -- End of tebako patch -- */
+
 /* define system APIs */
 #ifdef _WIN32
 #undef open
